# Copy to .env and fill in your RapidAPI key.
# Get one at https://rapidapi.com/bhagavad-gita-bhagavad-gita-default/api/bhagavad-gita3
# Server-only. Never prefix with NEXT_PUBLIC_ — that would inline it into the browser bundle.
RAPIDAPI_KEY=""

# Canonical origin, used as `metadataBase` so og:image URLs are absolute.
# Optional on Vercel, which supplies VERCEL_PROJECT_PRODUCTION_URL as the
# fallback. Anywhere else, set it before building: unset, it defaults to
# http://localhost:3000 and every share card ships a broken image.
SITE_URL="https://example.com"
